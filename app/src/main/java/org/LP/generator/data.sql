CREATE TABLE Houses (id INT AUTO_INCREMENT PRIMARY KEY, Price DECIMAL(10,2), Distance DECIMAL(10,2), Places INT)
CREATE TABLE Students (id INT AUTO_INCREMENT PRIMARY KEY, Budget DECIMAL(10,2), MaxDis DECIMAL(10,2))
